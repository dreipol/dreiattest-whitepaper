# dreiAttest Whitepaper

## Usage

- `$ git clone git@github.com:dreipol/dreiattest-whitepaper.git`
- `$ pipenv install` (make sure you have [pipenv](https://github.com/pypa/pipenv#installation) installed)
- `$ pipenv run build` 

